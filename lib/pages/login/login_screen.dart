import 'package:flutter/material.dart';
import 'package:flutter_authentication_ui/pages/login/components/body.dart';

class LoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}